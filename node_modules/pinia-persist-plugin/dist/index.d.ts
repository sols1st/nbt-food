import { PiniaPluginContext } from 'pinia';

interface Options {
    /**
     * which store to persist the state
     */
    storage?: Storage;
    /**
     * which key to persist the state
     */
    storageKey?: string;
    /**
     * store id[s] to omit persist
     */
    omits?: string[] | string;
}
declare const createPersistPlugin: (options?: Options | undefined) => (context: PiniaPluginContext) => void;

declare const resetStore: (omits?: (string[] | string)) => void;

export { Options, createPersistPlugin, resetStore };
